历史会话：
{history_text}

当前课程：{course_name}

当前用户问题：{query}

请结合历史会话，把当前用户问题改写成一个独立完整的课程学习问题。

要求：
1. 如果用户使用“它、这个、上面那题、刚才的方法、这一步”等指代表达，请结合历史会话补全指代对象。
2. 不要改变用户原本的意图；如果当前问题已经完整，直接保留原问题。
3. 不要抽取商品名，不要返回 item_names。
4. 只返回 JSON，不要输出解释。

JSON 格式如下：
{{
  "rewritten_query": "改写后的完整问题"
}}
